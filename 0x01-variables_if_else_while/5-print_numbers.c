#include<stdio.h>
/**
 * main - where the main code begins
 * Return: returns void value
 */
int main(void)
{
	int num;

	for (num = 48; num <= 57; num++)
	{
		putchar(num);
	}
	putchar('\n');
	return (0);
}

